@model IEnumerable<AreasDataBase.Models.District>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameDistrict)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.City)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameDistrict)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.City.NameCity)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdDistrict">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdDistrict">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdDistrict">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
